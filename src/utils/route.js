export function buildMapsRoute({origin,points}){if(!points||!points.length)return'';const c=points.filter(p=>p.lat&&p.lon);if(!c.length)return'';const d=c[0];const w=c.slice(1,10);const o=origin?.lat&&origin?.lon?`${origin.lat},${origin.lon}`:'';const ds=`${d.lat},${d.lon}`;const ws=w.map(x=>`${x.lat},${x.lon}`).join('|');const url=new URL('https://www.google.com/maps/dir/');url.searchParams.set('api','1');if(o)url.searchParams.set('origin',o);url.searchParams.set('destination',ds);if(ws)url.searchParams.set('waypoints',ws);return url.toString()}